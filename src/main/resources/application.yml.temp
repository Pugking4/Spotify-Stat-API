server:
  address: replace_with_local_ip
  port: repace_with_port
management:
  endpoints:
    web:
      exposure:
        include: mappings
database:
  username: replace_with_db_username
  password: replace_with_db_password